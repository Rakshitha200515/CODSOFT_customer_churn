I have used reactjs for frontend 
and flask for the backend 
app.js and app.css contributes to frontend
app.py and train.py does the backend part with training the model 
churn_model.joblib  this one is generated after the training of the model
